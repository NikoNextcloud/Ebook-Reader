export default function AudioPlayer({status,progress,onPlay,onPause,onStop,onRestart,disabled}){const active=status!=='idle'&&status!=='stopped'&&status!=='finished';return <div className={`player ${active?'live':''}`}><div className="player-info"><span className="cover">V</span><div><b>{active?'Voxora чете':'Готово за слушане'}</b><small>{status==='paused'?'На пауза':active?'Твоят текст оживява':'Избери настройки и започни'}</small></div></div><div className="progress"><i style={{width:`${progress}%`}}/></div><div className="player-actions"><button aria-label="Отначало" onClick={onRestart} disabled={disabled}>↶</button>{status==='speaking'?<button className="main-control" onClick={onPause}>Ⅱ</button>:<button className="main-control" onClick={onPlay} disabled={disabled}>▶</button>}<button aria-label="Стоп" onClick={onStop} disabled={!active}>■</button><span>{Math.round(progress)}%</span></div></div>}
